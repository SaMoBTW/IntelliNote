# Copy this file to `.env.local` and fill in real values.
# Supabase dashboard → your project → Project Settings → API
#
# Never commit `.env.local`. Keys are not read from application source code.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
